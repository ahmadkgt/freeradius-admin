services:
  db:
    image: mysql:8.4
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: radius
      MYSQL_USER: radius
      MYSQL_PASSWORD: radiuspass
    ports:
      - "3306:3306"
    volumes:
      - db-data:/var/lib/mysql
      - ./db:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uradius", "-pradiuspass"]
      interval: 5s
      timeout: 5s
      retries: 20

  backend:
    build:
      context: ./backend
    restart: unless-stopped
    environment:
      DATABASE_URL: mysql+pymysql://radius:radiuspass@db:3306/radius?charset=utf8mb4
      CORS_ORIGINS: "http://localhost:5173,http://localhost:8080"
      WHATSAPP_GATEWAY_URL: "http://whatsapp:3001"
      WHATSAPP_API_KEY: "${WHATSAPP_API_KEY:-dev-whatsapp-key}"
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy

  whatsapp:
    build:
      context: ./whatsapp
    restart: unless-stopped
    environment:
      PORT: "3001"
      AUTH_DIR: /app/auth
      WHATSAPP_API_KEY: "${WHATSAPP_API_KEY:-dev-whatsapp-key}"
      LOG_LEVEL: info
    volumes:
      - whatsapp-auth:/app/auth

  frontend:
    build:
      context: ./frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - backend

volumes:
  db-data:
  whatsapp-auth:
